---
name: skill-style
description: 创建或修改任何 SKILL.md 前必须读取并遵守本 skill。按全生命周期约束技能质量：新建给骨架与红线，修改保证新增内容不带病，优化按类型逐项瘦身，并用 scripts/lint.py 机检红线。凡要写新技能、改技能、给技能瘦身/review/查废话，哪怕只是"帮我看看这个 skill 写得行不行"，都必须触发本 skill。
---

# Skill Style

约束 SKILL.md 的写作质量：正文只装可执行指令与验收标准，装不下的进 references/ 或 scripts/。

## 与 skill-creator 的分工

创作流程（用户访谈、测试用例、评估循环）走 skill-creator；本技能定内容红线，作为入库前门禁，两者串联使用。

## 红线（违规必改）

| 编号 | 规则 | 判定方式 |
|------|------|----------|
| R1 | frontmatter 含 name/description，name 与目录名一致 | 机检 |
| R2 | description 只写使用场景与口语触发词，≤300 字符，不写背景故事 | 机检+人工 |
| R3 | SKILL.md 全文 ≤60 行，硬上限 120 行 | 机检 |
| R4 | 不写哲学句：动机、理念、生态价值类叙述一律删 | 机检特征词+人工 |
| R5 | 规则可判定：能回答"违反了没"；不可度量表述改硬阈值或删，特征词表见 scripts/lint.py 常量 | 机检特征词+人工 |
| R6 | 规则只在一处定义，SKILL.md 与 references/scripts 不互相复制语句（模板示例除外） | 机检+人工 |
| R7 | 文中相对链接指向的文件必须存在 | 机检 |

## 写作规范

- 每条以动作开头，用祈使句；"解释"只在它是验收标准时保留，例："自包含：读者无本会话上下文也能看懂"。
- description 埋三类触发词：正常场景、显式点名语、易混淆的近义说法。
- 输出物路径与命名写死为常量格式，不留开放式描述。
- 章节骨架二选一：任务型技能（替用户干活）用"触发时机 → 流程（编号步骤）→ 输出规范 → 边界"；治理型技能（约束其它技能怎么写）用"定位 → 规则 → 工作流 → 边界"。两者的触发场景都写在 description，不设重复小节。

## 工作流

### 创建

1. 需要访谈、测试用例、评估循环时先用 skill-creator 出稿；简单技能直接起稿。
2. 按"章节骨架"起草，落笔前对照红线逐条自查。
3. 跑 `python <本技能目录>/scripts/lint.py <技能目录>`，错误清零后交付。

### 修改

1. 只约束新增内容：新增句子同样受红线约束；历史遗留违规仅口头提示，不顺手改。
2. 改完跑 lint。

### 优化（瘦身）

按序逐项处理，输出修改清单（每项一句理由）：

1. 删哲学句（R4）；不可度量表述改硬阈值或删（R5）；
2. 跨文件重复语句合并到唯一定义处（R6）；
3. 单选项占位符改为字面值；
4. 超 R3 预算时把细节拆入 references/ 或 scripts/，正文留指针。

## 边界

- lint 只做机检；语义判断（是否哲学句）由执行者按红线清单人工判。
- 优化阶段只删减合并，不新增功能或改变技能行为。
