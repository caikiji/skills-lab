---
name: routing-easywork
description: 当智能体需要选择正确的技能，或检测到项目类型需要加载前置技能时使用——包括 Go 项目自动加载工具策略、bug 巡查等技能的路由和前置触发。
---

# Routing Easywork

帮助智能体根据任务类型选择正确的技能，避免内联处理本应由专门技能完成的工作。

此技能是路由器，只负责选择和调用，不替代下游技能的工作流。

## 路由规则

1. 判断当前项目类型，加载对应的前置技能（见下方前置技能表）
2. 判断任务是否属于本仓库的某个技能
3. 如果是，在路由表中查找并立即调用对应技能
4. 如果不是，不使用此技能

**必须先路由再继续**——不要因为查询看起来简单就跳过路由、直接内联处理。

## 前置技能

前置技能按项目类型自动加载，无需用户触发。检测到项目特征后**必须立即调用**，不可跳过。

| 项目特征 | 前置技能 | 作用 |
|---------|---------|------|
| 存在 `go.mod` 或工作区含 Go 模块 | `developing-go` | 加载 gopls/repomix 工具使用策略，避免低效的 Grep/全量 Read |

前置技能与路由表中的技能不互斥——Go 项目中执行 bug 巡查时，`developing-go` 和 `sweeping-bugs` 都应加载。

## 路由表

| 任务类型 | 技能 |
|---------|------|
| 对仓库执行系统性 bug 巡查、代码审查或质量审计 | `sweeping-bugs` |

## 非目标

- 不是每次对话必走的通用启动技能（前置技能除外）
- 不是下游技能工作流的替代品
- 不是通用规划或实现技能

## 红旗

| 想法 | 应对 |
|------|------|
| "只是查一下 bug，我直接看就行。" | 先路由。bug 巡查有专门工作流，应通过 `sweeping-bugs` 执行。 |
| "Go 项目直接 Grep 就够了。" | `developing-go` 是 Go 项目前置技能，必须加载。它提供的决策树比盲目 Grep 高效得多。 |
| "我知道该用哪个工具，不需要指导。" | 前置技能不可跳过。`developing-go` 的踩坑清单和优先级原则能避免常见错误。 |
| "我已经知道该用哪个技能，可以即兴发挥。" | 严格遵循路由规则，然后调用选中的技能。 |

## 交接规则

- 立即调用选中的技能
- 不要将 `Inference`、`Open Question` 或 `Decision Blocker` 简化为已确认事实
